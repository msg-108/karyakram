# Django
SECRET_KEY=change-me-to-a-real-secret-key
ALLOWED_HOSTS=localhost,127.0.0.1

# Database
DB_NAME=karyakram_db
DB_USER=karyakram_user
DB_PASSWORD=change-me
DB_HOST=localhost
DB_PORT=5432

# Encryption (generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())")
FIELD_ENCRYPTION_KEY=

# Email (Gmail SMTP — use App Password, not account password)
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-app-password

# Site URL (used in email template links)
SITE_URL=http://localhost:5173

# Dedicated QR JWT Signing Key
QR_JWT_SECRET_KEY=change-me-to-a-dedicated-qr-signing-key
QR_KEY_ROLLOUT_DATE=2026-08-06T00:00:00Z
